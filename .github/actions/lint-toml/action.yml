name: Lint TOML
description: "Linter to enforce TOML style and conventions"

runs:
  using: "composite"
  steps:
    - name: Install taplo
      uses: taiki-e/install-action@taplo

    - name: Run taplo-cli
      shell: bash
      run: taplo fmt --check
